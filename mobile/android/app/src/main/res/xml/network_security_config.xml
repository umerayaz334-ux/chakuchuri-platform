<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release builds require HTTPS unless a host is explicitly allowlisted. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>

# Phase history

Implementation notes from earlier build phases. Numbering uses `10b` / `11b` / `12b` where two topics shared the same phase number.

Current status lives in [`../roadmap-progress.md`](../roadmap-progress.md).
Live ops live in [`../ops/`](../ops/).
